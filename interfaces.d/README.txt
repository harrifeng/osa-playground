NOTE:  These configuration files are for reference.  These configuration files
       contain IP addresses that need to be changed to match your 
       configuration.  Also, these files will work on ONE node with the IPs 
       that are included.  You need to review the configuration before you 
       create the interfaces and bridges.
